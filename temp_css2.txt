.quiz-container {
  max-width: 800px;
  margin: 0 auto;
}

.quiz-header {
  text-align: center;
  margin-bottom: var(--spacing-xl);
}

.quiz-header h1 {
  margin-bottom: var(--spacing-md);
}

.quiz-intro {
  font-size: 1.1rem;
  color: var(--color-text-secondary);
  max-width: 600px;
  margin: 0 auto;
}

.quiz-disclaimer-top {
  max-width: 600px;
  margin: var(--spacing-md) auto 0;
  padding: var(--spacing-sm) var(--spacing-md);
  background: #FFF9E6;
  border-left: 4px solid #FF9800;
  border-radius: var(--radius-sm);
  font-size: 0.95rem;
  font-weight: 500;
}

.quiz-question {
  display: none;
  opacity: 0;
  transition: opacity 0.3s ease;
  margin-bottom: var(--spacing-xl);
}

.quiz-question.active {
  display: block;
  opacity: 1;
}

.quiz-question h2 {
  font-size: 1.5rem;
  margin-bottom: var(--spacing-lg);
  color: var(--color-text);
}

.quiz-options {
  display: grid;
  gap: var(--spacing-sm);
  margin-bottom: var(--spacing-xl);
}

.quiz-option {
  padding: var(--spacing-md);
  background: var(--color-surface);
  border: 2px solid var(--color-border);
  border-radius: var(--radius-md);
  font-size: 1rem;
  cursor: pointer;
  transition: all 0.2s ease;
  text-align: left;
}

.quiz-option:hover {
  border-color: var(--color-primary);
  background: #E3F2FD;
}

.quiz-option.selected {
  border-color: var(--color-primary);
  background: var(--color-primary);
  color: white;
  font-weight: 500;
}

.quiz-progress {
  margin-top: var(--spacing-xl);
  padding-top: var(--spacing-lg);
  border-top: 1px solid var(--color-border);
}

.progress-bar {
  width: 100%;
  height: 8px;
  background: var(--color-border);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: var(--spacing-sm);
}

.progress-fill {
  height: 100%;
  background: var(--color-primary);
  transition: width 0.3s ease;
}

.progress-text {
  text-align: center;
  color: var(--color-text-secondary);
  font-size: 0.9rem;
}

.quiz-results {
  padding: var(--spacing-xl);
  background: linear-gradient(135deg, #E3F2FD 0%, #FFF9C4 100%);
  border-radius: var(--radius-lg);
}

.quiz-results h2 {
  text-align: center;
  margin-bottom: var(--spacing-lg);
}

.results-section {
  margin: var(--spacing-xl) 0;
}

.results-section h3 {
  margin-bottom: var(--spacing-md);
  color: var(--color-text);
}

.matched-personas {
  display: grid;
  gap: var(--spacing-md);
}

.persona-card {
  background: white;
  padding: var(--spacing-md);
  border-radius: var(--radius-md);
  border: 1px solid var(--color-border);
}

.persona-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: var(--spacing-sm);
  gap: var(--spacing-sm);
}

.persona-header h4 {
  margin: 0;
  flex: 1;
}

.match-score {
  background: var(--color-primary);
  color: white;
  padding: 0.25rem 0.5rem;
  border-radius: var(--radius-sm);
  font-size: 0.85rem;
  font-weight: 600;
  white-space: nowrap;
}

.match-criteria {
  margin: var(--spacing-sm) 0;
  padding: var(--spacing-sm) 0;
  border-top: 1px solid var(--color-border);
}

.match-label {
  font-size: 0.85rem;
  color: var(--color-text-secondary);
  margin-bottom: 0.25rem;
  font-weight: 500;
}

.match-badges {
  display: flex;
  flex-wrap: wrap;
  gap: 0.35rem;
}

.match-badge {
  display: inline-block;
  padding: 0.25rem 0.5rem;
  border-radius: var(--radius-sm);
  font-size: 0.75rem;
  font-weight: 500;
  background: #E3F2FD;
  color: #1565C0;
  border: 1px solid #90CAF9;
}

.match-badge-priority {
  background: #E8F5E9;
  color: #2E7D32;
  border-color: #81C784;
}

.match-badge-concern {
  background: #FFF3E0;
  color: #E65100;
  border-color: #FFB74D;
}

.match-badge-governmentRole, .match-badge-urgency, .match-badge-economicView {
  background: #E1F5FE;
  color: #01579B;
  border-color: #81D4FA;
}

.match-badge-age {
  background: #F3E5F5;
  color: #6A1B9A;
  border-color: #BA68C8;
}

.match-badge-work {
  background: #E0F2F1;
  color: #00695C;
  border-color: #4DB6AC;
}

.match-badge-income, .match-badge-location, .match-badge-family, .match-badge-education {
  background: #FFF9C4;
  color: #F57F17;
  border-color: #FDD835;
}

.btn-link {
  color: var(--color-primary);
  text-decoration: none;
  font-weight: 500;
  display: inline-block;
  margin-top: var(--spacing-sm);
}

.btn-link:hover {
  text-decoration: underline;
}

.results-actions {
  display: flex;
  gap: var(--spacing-md);
  justify-content: center;
  margin: var(--spacing-xl) 0;
  flex-wrap: wrap;
}

.btn {
  padding: var(--spacing-sm) var(--spacing-lg);
  border-radius: var(--radius-md);
  font-weight: 500;
  text-decoration: none;
  border: none;
  cursor: pointer;
  font-size: 1rem;
  transition: all 0.2s ease;
}

.btn-primary {
  background: var(--color-primary);
  color: white;
}

.btn-primary:hover {
  background: #0D47A1;
}

.btn-secondary {
  background: var(--color-border);
  color: var(--color-text);
}

.btn-secondary:hover {
  background: #ccc;
}

.results-disclaimer {
  background: white;
  padding: var(--spacing-md);
  border-left: 4px solid var(--color-secondary);
  border-radius: var(--radius-sm);
  font-size: 0.9rem;
  margin-top: var(--spacing-lg);
}

/* Detailed persona card styles */
.persona-card-detailed {
  background: white;
  padding: var(--spacing-lg);
  border-radius: var(--radius-md);
  border: 2px solid var(--color-border);
  margin-bottom: var(--spacing-lg);
}

/* Persona phrase in quiz results */
.persona-phrase-quiz {
  margin-top: var(--spacing-md);
  padding: var(--spacing-sm) var(--spacing-md);
  background: var(--color-surface);
  border-left: 3px solid var(--color-primary);
  border-radius: var(--radius-sm);
}

.persona-phrase-quiz em {
  font-size: 0.95rem;
  color: var(--color-text);
  font-style: italic;
  line-height: 1.5;
}

/* Persona demographics in quiz results */
.persona-demographics-quiz {
  display: flex;
  flex-wrap: wrap;
  gap: 0.5rem;
  margin-top: var(--spacing-sm);
  padding: var(--spacing-sm);
  background: var(--color-surface);
  border-radius: var(--radius-sm);
}

.demo-item {
  font-size: 0.85rem;
  color: var(--color-text);
  padding: 0.25rem 0;
}

/* Persona context in quiz */
.persona-context-quiz {
  margin-top: var(--spacing-md);
  padding-top: var(--spacing-md);
  border-top: 1px solid var(--color-border);
}

.persona-context-quiz h5 {
  font-size: 0.95rem;
  margin-bottom: var(--spacing-sm);
  color: var(--color-text);
  font-weight: 600;
}

.context-list-compact {
  list-style: none;
  padding: 0;
  margin: 0;
  font-size: 0.85rem;
  line-height: 1.6;
}

.context-list-compact li {
  padding-left: 1.2rem;
  position: relative;
  margin-bottom: 0.25rem;
}

.context-list-compact li:before {
  content: "•";
  position: absolute;
  left: 0;
  color: var(--color-primary);
  font-weight: bold;
}

/* Compact vote intention (1 line, 3 boxes) */
.vote-intention-compact {
  margin-top: var(--spacing-md);
  padding-top: var(--spacing-md);
  border-top: 1px solid var(--color-border);
}

.vote-boxes-compact {
  display: flex;
  gap: 0.4rem;
  flex-wrap: wrap;
}

.vote-box-compact {
  flex: 1;
  min-width: 120px;
  display: flex;
  flex-direction: column;
  align-items: center;
  padding: 0.5rem 0.4rem;
  background: var(--color-surface);
  border-radius: var(--radius-sm);
  border: 1.5px solid var(--color-border);
  transition: all 0.2s ease;
}

.vote-box-compact:hover {
  border-color: var(--color-primary);
  box-shadow: 0 2px 6px rgba(0,0,0,0.08);
}

.vote-box-rank {
  font-size: 0.7rem;
  font-weight: 700;
  color: var(--color-text-secondary);
  margin-bottom: 0.2rem;
}

.vote-box-candidate {
  font-size: 0.8rem;
  font-weight: 600;
  text-align: center;
  margin-bottom: 0.35rem;
  line-height: 1.2;
}

.vote-box-rating {
  font-weight: 700;
  padding: 0.25rem 0.6rem;
  border-radius: var(--radius-sm);
  font-size: 0.85rem;
}

/* Rating colors - matching matriz */
.rating-high {
  background: #C8E6C9;
  color: #2E7D32;
}

.rating-medium {
  background: #FFF9C4;
  color: #F57F17;
}

.rating-low {
  background: #FFCCBC;
  color: #D84315;
}

.rating-poor {
  background: #FFCDD2;
  color: #C62828;
}

.btn-link-detailed {
  display: inline-block;
  margin-top: var(--spacing-md);
  color: var(--color-primary);
  text-decoration: none;
  font-weight: 500;
  font-size: 0.9rem;
}

.btn-link-detailed:hover {
  text-decoration: underline;
}

@media (max-width: 768px) {
  .quiz-option {
    padding: var(--spacing-sm);
  }

  .candidate-card {
    flex-direction: column;
    text-align: center;
  }

  .results-actions {
    flex-direction: column;
  }

  .btn {
    width: 100%;
  }
}
</style>
