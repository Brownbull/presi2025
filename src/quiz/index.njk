---
layout: layouts/base.njk
title: Encuentra Personas Parecidas a Ti
description: Responde 7 preguntas y descubre qué personas ficticias son similares a tu perfil
---

<div class="quiz-container">
  <div class="quiz-header">
    <h1>🎯 Encuentra Personas Parecidas a Ti</h1>
    <p class="quiz-intro">
      Responde <strong>7 preguntas</strong> sobre tu situación y prioridades.
      Te mostraremos qué <strong>personas electorales ficticias</strong> (perfiles creados para este estudio)
      son más similares a ti y cómo evaluaron los programas presidenciales según su contexto y necesidades.
    </p>
    <p class="quiz-disclaimer-top">
      ℹ️ Este es un ejercicio informativo con fines educativos. Las personas son ficticias y esto NO constituye recomendación electoral.
    </p>
  </div>

  <div id="quiz-content">
    <!-- Question 1: Age -->
    <div class="quiz-question active" data-question="1">
      <h2>1. ¿En qué rango de edad estás?</h2>
      <div class="quiz-options">
        <button class="quiz-option" data-value="18-25">18-25 años</button>
        <button class="quiz-option" data-value="26-35">26-35 años</button>
        <button class="quiz-option" data-value="36-50">36-50 años</button>
        <button class="quiz-option" data-value="51+">51+ años</button>
      </div>
    </div>

    <!-- Question 2: Main Concern -->
    <div class="quiz-question" data-question="2">
      <h2>2. ¿Cuál es tu principal preocupación?</h2>
      <div class="quiz-options">
        <button class="quiz-option" data-value="salud">Salud</button>
        <button class="quiz-option" data-value="educacion">Educación</button>
        <button class="quiz-option" data-value="seguridad">Seguridad</button>
        <button class="quiz-option" data-value="economia">Economía y Empleo</button>
        <button class="quiz-option" data-value="vivienda">Vivienda</button>
        <button class="quiz-option" data-value="pensiones">Pensiones</button>
      </div>
    </div>

    <!-- Question 3: Work Situation -->
    <div class="quiz-question" data-question="3">
      <h2>3. ¿Cuál es tu situación laboral?</h2>
      <div class="quiz-options">
        <button class="quiz-option" data-value="dependiente">Trabajador dependiente</button>
        <button class="quiz-option" data-value="independiente">Trabajador independiente / Emprendedor</button>
        <button class="quiz-option" data-value="profesional">Profesional (universidad)</button>
        <button class="quiz-option" data-value="estudiante">Estudiante</button>
        <button class="quiz-option" data-value="jubilado">Jubilado/a</button>
        <button class="quiz-option" data-value="duena-casa">Dueña de casa</button>
      </div>
    </div>

    <!-- Question 4: Income Level -->
    <div class="quiz-question" data-question="4">
      <h2>4. ¿En qué rango está tu ingreso familiar mensual?</h2>
      <div class="quiz-options">
        <button class="quiz-option" data-value="bajo">Menos de $500.000</button>
        <button class="quiz-option" data-value="medio-bajo">$500.000 - $1.000.000</button>
        <button class="quiz-option" data-value="medio">$1.000.000 - $2.000.000</button>
        <button class="quiz-option" data-value="medio-alto">$2.000.000 - $3.500.000</button>
        <button class="quiz-option" data-value="alto">Más de $3.500.000</button>
      </div>
    </div>

    <!-- Question 5: Location -->
    <div class="quiz-question" data-question="5">
      <h2>5. ¿Dónde vives?</h2>
      <div class="quiz-options">
        <button class="quiz-option" data-value="santiago-oriente">Santiago (sector oriente)</button>
        <button class="quiz-option" data-value="santiago-sur">Santiago (sector sur)</button>
        <button class="quiz-option" data-value="santiago-otro">Santiago (otras comunas)</button>
        <button class="quiz-option" data-value="region-urbano">Región (ciudad)</button>
        <button class="quiz-option" data-value="region-rural">Región (zona rural)</button>
      </div>
    </div>

    <!-- Question 6: Family Situation -->
    <div class="quiz-question" data-question="6">
      <h2>6. ¿Cuál es tu situación familiar?</h2>
      <div class="quiz-options">
        <button class="quiz-option" data-value="solo">Vivo solo/a</button>
        <button class="quiz-option" data-value="pareja">En pareja sin hijos</button>
        <button class="quiz-option" data-value="familia-joven">Familia con hijos pequeños</button>
        <button class="quiz-option" data-value="familia-escolar">Familia con hijos en edad escolar</button>
        <button class="quiz-option" data-value="familia-adulta">Hijos adultos / Adulto mayor</button>
      </div>
    </div>

    <!-- Question 7: Top Priority -->
    <div class="quiz-question" data-question="7">
      <h2>7. Si pudieras cambiar UNA cosa en Chile, ¿cuál sería?</h2>
      <div class="quiz-options">
        <button class="quiz-option" data-value="delincuencia">Reducir la delincuencia</button>
        <button class="quiz-option" data-value="costo-vida">Bajar el costo de vida</button>
        <button class="quiz-option" data-value="salud-publica">Mejorar salud pública</button>
        <button class="quiz-option" data-value="educacion-calidad">Mejorar calidad educación</button>
        <button class="quiz-option" data-value="pensiones-dignas">Pensiones dignas</button>
        <button class="quiz-option" data-value="medio-ambiente">Proteger medio ambiente</button>
        <button class="quiz-option" data-value="oportunidades">Más oportunidades laborales</button>
      </div>
    </div>

    <!-- Results Section -->
    <div id="quiz-results" class="quiz-results" style="display: none;">
      <h2>✨ Tus Resultados</h2>

      <div class="results-section">
        <h3>Personas que se parecen a ti:</h3>
        <div id="matched-personas" class="matched-personas"></div>
      </div>

      <div class="results-actions">
        <a href="{{ '/matriz/' | url }}" class="btn btn-primary">Ver matriz completa</a>
        <button id="restart-quiz" class="btn btn-secondary">Repetir quiz</button>
      </div>

      <p class="results-disclaimer">
        ⚠️ <strong>Este es un ejercicio informativo con fines educativos.</strong>
        Los resultados muestran coincidencias entre tu perfil y personas ficticias creadas para este estudio.
        Este contenido NO constituye recomendación electoral ni debe ser la única fuente para decisiones de voto.
        Te invitamos a leer los programas completos y consultar múltiples fuentes antes de votar.
      </p>
    </div>
  </div>

  <div class="quiz-progress">
    <div class="progress-bar">
      <div id="progress-fill" class="progress-fill" style="width: 0%"></div>
    </div>
    <p class="progress-text">Pregunta <span id="current-question">0</span> de 7</p>
  </div>
</div>

<script>
  // Quiz State
  let answers = {
    age: null,
    concern: null,
    work: null,
    income: null,
    location: null,
    family: null,
    priority: null
  };

  let currentQuestion = 0;
  const totalQuestions = 7;

  // Comprehensive persona database with matching criteria
  const personasDB = {
    '01-maria-jose-contreras': { age: '26-35', concern: 'salud', work: 'profesional', income: 'medio-alto', priority: 'salud-publica' },
    '02-juan-carlos-munoz': { age: '51+', concern: 'pensiones', work: 'jubilado', income: 'bajo', priority: 'pensiones-dignas' },
    '03-luis-hernandez': { age: '36-50', concern: 'seguridad', work: 'independiente', income: 'medio-bajo', priority: 'delincuencia' },
    '04-catalina-rojas': { age: '18-25', concern: 'educacion', work: 'estudiante', income: 'medio-bajo', priority: 'educacion-calidad' },
    '05-rosa-perez': { age: '51+', concern: 'seguridad', work: 'independiente', income: 'medio-bajo', priority: 'delincuencia' },
    '06-pedro-quintana': { age: '36-50', concern: 'economia', work: 'dependiente', income: 'medio', priority: 'oportunidades' },
    '07-daniela-soto': { age: '26-35', concern: 'salud', work: 'profesional', income: 'medio', priority: 'salud-publica' },
    '08-jose-miguel-valdes': { age: '51+', concern: 'seguridad', work: 'independiente', income: 'medio', priority: 'delincuencia' },
    '09-alejandra-martinez': { age: '26-35', concern: 'economia', work: 'profesional', income: 'medio-alto', priority: 'oportunidades' },
    '10-carmen-huaiquilaf': { age: '36-50', concern: 'educacion', work: 'duena-casa', income: 'bajo', priority: 'educacion-calidad' },
    '11-carla-espinoza': { age: '36-50', concern: 'economia', work: 'dependiente', income: 'medio-bajo', priority: 'costo-vida' },
    '12-roberto-gonzalez': { age: '36-50', concern: 'seguridad', work: 'profesional', income: 'alto', priority: 'delincuencia' },
    '13-carlos-barria': { age: '51+', concern: 'economia', work: 'independiente', income: 'medio-bajo', priority: 'oportunidades' },
    '14-yohana-rodriguez': { age: '26-35', concern: 'economia', work: 'dependiente', income: 'medio-bajo', priority: 'oportunidades' },
    '15-miguel-angel-castro': { age: '51+', concern: 'vivienda', work: 'dependiente', income: 'bajo', priority: 'costo-vida' },
    '16-francisco-valdes': { age: '36-50', concern: 'economia', work: 'independiente', income: 'medio-alto', priority: 'oportunidades' },
    '17-javiera-mella': { age: '26-35', concern: 'economia', work: 'independiente', income: 'bajo', priority: 'costo-vida' },
    '18-patricia-morales': { age: '36-50', concern: 'vivienda', work: 'dependiente', income: 'medio', priority: 'costo-vida' },
    '19-matias-contreras': { age: '18-25', concern: 'salud', work: 'estudiante', income: 'medio-bajo', priority: 'salud-publica' },
    '20-hector-silva': { age: '36-50', concern: 'economia', work: 'dependiente', income: 'bajo', priority: 'oportunidades' },
    '21-andrea-fuentes': { age: '36-50', concern: 'salud', work: 'duena-casa', income: 'medio-bajo', priority: 'salud-publica' },
    '22-diego-ramirez': { age: '18-25', concern: 'economia', work: 'estudiante', income: 'medio', priority: 'oportunidades' },
    '23-gonzalo-larrain': { age: '36-50', concern: 'economia', work: 'profesional', income: 'alto', priority: 'oportunidades' },
    '24-bryan-flores': { age: '18-25', concern: 'educacion', work: 'estudiante', income: 'bajo', priority: 'oportunidades' },
    '25-dr-sebastian-herrera': { age: '26-35', concern: 'salud', work: 'profesional', income: 'medio-alto', priority: 'salud-publica' },
    '26-isabel-parra': { age: '51+', concern: 'educacion', work: 'profesional', income: 'medio', priority: 'educacion-calidad' },
    '27-manuel-gutierrez': { age: '36-50', concern: 'seguridad', work: 'dependiente', income: 'medio-bajo', priority: 'delincuencia' },
    '28-camila-oyarzun': { age: '26-35', concern: 'economia', work: 'profesional', income: 'medio-bajo', priority: 'medio-ambiente' }
  };

  // Event listeners for quiz options
  document.querySelectorAll('.quiz-option').forEach(button => {
    button.addEventListener('click', function() {
      const questionDiv = this.closest('.quiz-question');
      const questionNum = questionDiv.dataset.question;
      const value = this.dataset.value;

      // Store answer
      const questionKeys = ['age', 'concern', 'work', 'income', 'location', 'family', 'priority'];
      answers[questionKeys[questionNum - 1]] = value;

      // Visual feedback
      questionDiv.querySelectorAll('.quiz-option').forEach(opt => opt.classList.remove('selected'));
      this.classList.add('selected');

      // Move to next question
      setTimeout(() => {
        if (questionNum < totalQuestions) {
          showQuestion(parseInt(questionNum) + 1);
        } else {
          showResults();
        }
      }, 300);
    });
  });

  function showQuestion(num) {
    currentQuestion = num;

    // Hide all questions
    document.querySelectorAll('.quiz-question').forEach(q => q.classList.remove('active'));

    // Show current question
    const currentQ = document.querySelector(`[data-question="${num}"]`);
    if (currentQ) {
      currentQ.classList.add('active');
      currentQ.scrollIntoView({ behavior: 'smooth', block: 'start' });
    }

    // Update progress
    const progress = (num / totalQuestions) * 100;
    document.getElementById('progress-fill').style.width = progress + '%';
    document.getElementById('current-question').textContent = num;
  }

  function showResults() {
    // Hide questions
    document.querySelectorAll('.quiz-question').forEach(q => q.style.display = 'none');

    // Show results
    document.getElementById('quiz-results').style.display = 'block';
    document.getElementById('quiz-results').scrollIntoView({ behavior: 'smooth', block: 'start' });

    // Calculate matches
    const matchedPersonas = calculatePersonaMatches();

    // Display matched personas
    displayMatchedPersonas(matchedPersonas);

    // Update progress to 100%
    document.getElementById('progress-fill').style.width = '100%';
    document.getElementById('current-question').textContent = '7';
  }

  function calculatePersonaMatches() {
    // Score each persona based on answer matches
    const scores = [];

    for (const [slug, persona] of Object.entries(personasDB)) {
      let score = 0;
      const matches = [];

      // Priority question (question 7) - highest weight
      if (answers.priority === persona.priority) {
        score += 10;
        matches.push({ type: 'priority', label: 'Prioridad principal', weight: 10 });
      }

      // Main concern (question 2) - second highest weight
      if (answers.concern === persona.concern) {
        score += 8;
        matches.push({ type: 'concern', label: 'Preocupación principal', weight: 8 });
      }

      // Age (question 1) - important demographic
      if (answers.age === persona.age) {
        score += 6;
        matches.push({ type: 'age', label: 'Rango de edad', weight: 6 });
      }

      // Work situation (question 3)
      if (answers.work === persona.work) {
        score += 5;
        matches.push({ type: 'work', label: 'Situación laboral', weight: 5 });
      }

      // Income level (question 4)
      if (answers.income === persona.income) {
        score += 4;
        matches.push({ type: 'income', label: 'Nivel de ingreso', weight: 4 });
      }

      // Only include personas with some match
      if (score > 0) {
        scores.push({ slug, score, matches });
      }
    }

    // Sort by score descending
    scores.sort((a, b) => b.score - a.score);

    // Return top 3 matches with their match details
    return scores.slice(0, 3);
  }

  function displayMatchedPersonas(personaMatches) {
    const container = document.getElementById('matched-personas');

    if (personaMatches.length === 0) {
      container.innerHTML = '<p>No se encontraron coincidencias exactas. Te recomendamos explorar todas las personas en el sitio.</p>';
      return;
    }

    const personaNames = {
      '01-maria-jose-contreras': 'María José Contreras - La Profesional Agobiada',
      '02-juan-carlos-munoz': 'Juan Carlos Muñoz - El Jubilado Precarizado',
      '03-luis-hernandez': 'Luis Hernández - El Emprendedor de Subsistencia',
      '04-catalina-rojas': 'Catalina Rojas - La Universitaria Endeudada',
      '05-rosa-perez': 'Rosa Pérez - La Microempresaria Ahogada',
      '06-pedro-quintana': 'Pedro Quintana - El Obrero del Norte',
      '07-daniela-soto': 'Daniela Soto - La Funcionaria Pública Quemada',
      '08-jose-miguel-valdes': 'José Miguel Valdés - El Agricultor Amenazado',
      '09-alejandra-martinez': 'Alejandra Martínez - La Teletrabajadora Atrapada',
      '10-carmen-huaiquilaf': 'Carmen Huaiquilaf - La Madre Mapuche Urbana',
      '11-carla-espinoza': 'Carla Espinoza - La Vendedora Retail Precaria',
      '12-roberto-gonzalez': 'Roberto González - El Santiaguino Hastiado',
      '13-carlos-barria': 'Carlos Barría - El Pescador Artesanal Olvidado',
      '14-yohana-rodriguez': 'Yohana Rodríguez - La Migrante Integrada',
      '15-miguel-angel-castro': 'Miguel Ángel Castro - El Poblador Histórico',
      '16-francisco-valdes': 'Francisco Valdés - El Empresario PYME Sofisticado',
      '17-javiera-mella': 'Javiera Mella - La Artista Precarizada',
      '18-patricia-morales': 'Patricia Morales - La Deudora Habitacional',
      '19-matias-contreras': 'Matías Contreras - El Joven LGBTQ+',
      '20-hector-silva': 'Héctor Silva - El Temporal Agrícola',
      '21-andrea-fuentes': 'Andrea Fuentes - La Cuidadora Invisible',
      '22-diego-ramirez': 'Diego Ramírez - El Gamer Semi-Pro',
      '23-gonzalo-larrain': 'Gonzalo Larraín - El Gerente Desencantado',
      '24-bryan-flores': 'Bryan Flores - El Nini Atrapado',
      '25-dr-sebastian-herrera': 'Dr. Sebastián Herrera - El Médico Agotado',
      '26-isabel-parra': 'Isabel Parra - La Profe Militante',
      '27-manuel-gutierrez': 'Manuel Gutiérrez - El Chofer de Micro',
      '28-camila-oyarzun': 'Camila Oyarzún - La Activista Climática'
    };

    container.innerHTML = personaMatches.map(({ slug, score, matches }) => `
      <div class="persona-card">
        <div class="persona-header">
          <h4>${personaNames[slug] || slug}</h4>
          <span class="match-score">${score} pts</span>
        </div>
        <div class="match-criteria">
          <p class="match-label">Coincidencias:</p>
          <div class="match-badges">
            ${matches.map(match => `
              <span class="match-badge match-badge-${match.type}" title="${match.label}">
                ✓ ${match.label}
              </span>
            `).join('')}
          </div>
        </div>
        <a href="{{ '/personas/' | url }}${slug}/" class="btn-link">Ver evaluaciones completas →</a>
      </div>
    `).join('');
  }

  // Restart quiz
  document.addEventListener('click', function(e) {
    if (e.target && e.target.id === 'restart-quiz') {
      location.reload();
    }
  });

  // Initialize
  showQuestion(1);
</script>

<style>
.quiz-container {
  max-width: 800px;
  margin: 0 auto;
}

.quiz-header {
  text-align: center;
  margin-bottom: var(--spacing-xl);
}

.quiz-header h1 {
  margin-bottom: var(--spacing-md);
}

.quiz-intro {
  font-size: 1.1rem;
  color: var(--color-text-secondary);
  max-width: 600px;
  margin: 0 auto;
}

.quiz-disclaimer-top {
  max-width: 600px;
  margin: var(--spacing-md) auto 0;
  padding: var(--spacing-sm) var(--spacing-md);
  background: #FFF9E6;
  border-left: 4px solid #FF9800;
  border-radius: var(--radius-sm);
  font-size: 0.95rem;
  font-weight: 500;
}

.quiz-question {
  display: none;
  opacity: 0;
  transition: opacity 0.3s ease;
  margin-bottom: var(--spacing-xl);
}

.quiz-question.active {
  display: block;
  opacity: 1;
}

.quiz-question h2 {
  font-size: 1.5rem;
  margin-bottom: var(--spacing-lg);
  color: var(--color-text);
}

.quiz-options {
  display: grid;
  gap: var(--spacing-sm);
  margin-bottom: var(--spacing-xl);
}

.quiz-option {
  padding: var(--spacing-md);
  background: var(--color-surface);
  border: 2px solid var(--color-border);
  border-radius: var(--radius-md);
  font-size: 1rem;
  cursor: pointer;
  transition: all 0.2s ease;
  text-align: left;
}

.quiz-option:hover {
  border-color: var(--color-primary);
  background: #E3F2FD;
}

.quiz-option.selected {
  border-color: var(--color-primary);
  background: var(--color-primary);
  color: white;
  font-weight: 500;
}

.quiz-progress {
  margin-top: var(--spacing-xl);
  padding-top: var(--spacing-lg);
  border-top: 1px solid var(--color-border);
}

.progress-bar {
  width: 100%;
  height: 8px;
  background: var(--color-border);
  border-radius: 4px;
  overflow: hidden;
  margin-bottom: var(--spacing-sm);
}

.progress-fill {
  height: 100%;
  background: var(--color-primary);
  transition: width 0.3s ease;
}

.progress-text {
  text-align: center;
  color: var(--color-text-secondary);
  font-size: 0.9rem;
}

.quiz-results {
  padding: var(--spacing-xl);
  background: linear-gradient(135deg, #E3F2FD 0%, #FFF9C4 100%);
  border-radius: var(--radius-lg);
}

.quiz-results h2 {
  text-align: center;
  margin-bottom: var(--spacing-lg);
}

.results-section {
  margin: var(--spacing-xl) 0;
}

.results-section h3 {
  margin-bottom: var(--spacing-md);
  color: var(--color-text);
}

.matched-personas {
  display: grid;
  gap: var(--spacing-md);
}

.persona-card {
  background: white;
  padding: var(--spacing-md);
  border-radius: var(--radius-md);
  border: 1px solid var(--color-border);
}

.persona-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  margin-bottom: var(--spacing-sm);
  gap: var(--spacing-sm);
}

.persona-header h4 {
  margin: 0;
  flex: 1;
}

.match-score {
  background: var(--color-primary);
  color: white;
  padding: 0.25rem 0.5rem;
  border-radius: var(--radius-sm);
  font-size: 0.85rem;
  font-weight: 600;
  white-space: nowrap;
}

.match-criteria {
  margin: var(--spacing-sm) 0;
  padding: var(--spacing-sm) 0;
  border-top: 1px solid var(--color-border);
}

.match-label {
  font-size: 0.85rem;
  color: var(--color-text-secondary);
  margin-bottom: 0.25rem;
  font-weight: 500;
}

.match-badges {
  display: flex;
  flex-wrap: wrap;
  gap: 0.35rem;
}

.match-badge {
  display: inline-block;
  padding: 0.25rem 0.5rem;
  border-radius: var(--radius-sm);
  font-size: 0.75rem;
  font-weight: 500;
  background: #E3F2FD;
  color: #1565C0;
  border: 1px solid #90CAF9;
}

.match-badge-priority {
  background: #E8F5E9;
  color: #2E7D32;
  border-color: #81C784;
}

.match-badge-concern {
  background: #FFF3E0;
  color: #E65100;
  border-color: #FFB74D;
}

.match-badge-age {
  background: #F3E5F5;
  color: #6A1B9A;
  border-color: #BA68C8;
}

.match-badge-work {
  background: #E0F2F1;
  color: #00695C;
  border-color: #4DB6AC;
}

.match-badge-income {
  background: #FFF9C4;
  color: #F57F17;
  border-color: #FDD835;
}

.btn-link {
  color: var(--color-primary);
  text-decoration: none;
  font-weight: 500;
  display: inline-block;
  margin-top: var(--spacing-sm);
}

.btn-link:hover {
  text-decoration: underline;
}

.results-actions {
  display: flex;
  gap: var(--spacing-md);
  justify-content: center;
  margin: var(--spacing-xl) 0;
  flex-wrap: wrap;
}

.btn {
  padding: var(--spacing-sm) var(--spacing-lg);
  border-radius: var(--radius-md);
  font-weight: 500;
  text-decoration: none;
  border: none;
  cursor: pointer;
  font-size: 1rem;
  transition: all 0.2s ease;
}

.btn-primary {
  background: var(--color-primary);
  color: white;
}

.btn-primary:hover {
  background: #0D47A1;
}

.btn-secondary {
  background: var(--color-border);
  color: var(--color-text);
}

.btn-secondary:hover {
  background: #ccc;
}

.results-disclaimer {
  background: white;
  padding: var(--spacing-md);
  border-left: 4px solid var(--color-secondary);
  border-radius: var(--radius-sm);
  font-size: 0.9rem;
  margin-top: var(--spacing-lg);
}

@media (max-width: 768px) {
  .quiz-option {
    padding: var(--spacing-sm);
  }

  .candidate-card {
    flex-direction: column;
    text-align: center;
  }

  .results-actions {
    flex-direction: column;
  }

  .btn {
    width: 100%;
  }
}
</style>
